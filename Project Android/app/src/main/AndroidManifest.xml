<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ndiappink.myapplication">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Tugas Pemrograman Mobile"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.ndiappink.myapplication.ScanBarcode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TerimaData"/>
        <activity android:name=".IntentEksplisit"/>
        <activity android:name=".BarVolume"/>
        <activity android:name=".ViewGroupAndView"/>
        <activity android:name=".ListPresident"/>
        <activity android:name=".SimpleApp"/>
        <activity android:name=".Portrait"/>
    </application>

</manifest>